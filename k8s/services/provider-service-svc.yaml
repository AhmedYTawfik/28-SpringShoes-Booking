apiVersion: v1
kind: Service
metadata:
  name: provider-service
  namespace: booking
  labels:
    app: provider-service
spec:
  selector:
    app: provider-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
