apiVersion: v1
kind: Service
metadata:
  name: calendar-service
  namespace: booking
  labels:
    app: calendar-service
spec:
  selector:
    app: calendar-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
