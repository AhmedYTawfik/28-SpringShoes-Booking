apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: booking
  labels:
    app: invoice-service
spec:
  selector:
    app: invoice-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
