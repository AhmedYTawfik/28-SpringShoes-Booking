<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="invoice-service"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} correlationId=%X{correlationId} invoiceId=%X{invoiceId} bookingId=%X{bookingId} userId=%X{userId} routingKey=%X{routingKey} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>${LOKI_URL:http://loki:3100}/loki/api/v1/push</url>
        </http>
        <format>
            <label>
                <pattern>app=${appName},host=${HOSTNAME}</pattern>
            </label>
            <message>
                <pattern>level=%level correlationId=%X{correlationId} invoiceId=%X{invoiceId} bookingId=%X{bookingId} userId=%X{userId} routingKey=%X{routingKey} logger=%logger{36} - %msg%n</pattern>
            </message>
        </format>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOKI"/>
    </root>
</configuration>
